# T44 Regression

PASS — `饼有了，钱呢？`: semantic split is allowed; `钱呢？` may bind to the open palm and remains instantly readable.
PASS — `让我缓缓`: progressive size/spacing/baseline changes are allowed, but the phrase remains one continuous reading unit.
PASS — text color adapts or relocates when it collides with a same-colored tie.

P1 FAIL:
- one sentence is arbitrarily split into two distant text islands;
- viewer must search for the second half;
- text and tie/clothing merge visually;
- a large rectangular backing is added by default instead of solving placement/contrast first.
